import React, { useState, useEffect } from 'react'
import { getChirpsByUsername } from '../service/api-helper'


function UserChirps() {

    // const [userChirps, setUserChirps] = useState([])

    // const getUserChirps = async name => {
    //     const json = await getChirpsByUsername()
    //     console.log('user chirps json', json)
    //     const userChirpArr = user.filter(user => user.username === name)
    //     setUserChirps(userChirpArr)
    // }


    return (
        <>
            {/* {userChirps} */}
            <hr /></>
    )
}

export default UserChirps